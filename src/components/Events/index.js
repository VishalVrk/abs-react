import React, { Component } from 'react';
import CardListItem from '../utlis/CardListItem'

export default class Events extends Component {
    render() {
        return (
            <CardListItem data="events" heading="Organizational Events
            " ButtonName="Events"/>
        )
    }
}
